import { Routes } from '@angular/router';
import { TrackingTableComponent } from './tracking-table/tracking-table.component';

export const routes: Routes = [
  { path: '', component: TrackingTableComponent }
];
